# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root123
DB_NAME=WEB2_BookStore

# For Render.com deployment
# Render sẽ cung cấp DATABASE_URL, bạn cần parse nó
# DATABASE_URL=mysql://user:password@host:port/database
