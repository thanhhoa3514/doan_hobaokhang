# Database Configuration

# Option 1: Dùng DATABASE_URL (Khuyến nghị cho TiDB Cloud/Render)
# Format: mysql://username:password@host:port/database?ssl-mode=VERIFY_IDENTITY
DATABASE_URL=mysql://4TqR3xYz.root:YOUR_PASSWORD@gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/WEB2_BookStore?ssl-mode=VERIFY_IDENTITY

# Option 2: Dùng individual variables (cho Docker local)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root123
DB_NAME=WEB2_BookStore
DB_PORT=3306

# Option 3: Local development (XAMPP/WAMP)
# Không cần config gì, db_connect.php sẽ tự động dùng localhost

